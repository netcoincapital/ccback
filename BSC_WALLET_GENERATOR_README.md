# 🔐 تولیدکننده کیف پول BSC (BEP-20)

این اسکریپت برای تولید انبوه کیف پول‌های BSC (Binance Smart Chain) با استفاده از استاندارد BIP-39 طراحی شده است.

## ✨ ویژگی‌ها

- 🎯 تولید 100,000 کیف پول BSC
- 🔑 تولید 12 کلمه mnemonic برای هر کیف پول  
- 🔐 استخراج کلید خصوصی (Private Key)
- 📍 تولید آدرس عمومی (Public Address)
- 💾 ذخیره در فایل CSV
- 📊 نمایش پیشرفت real-time
- ⚡ پردازش دسته‌ای برای بهینه‌سازی

## 📋 پیش‌نیازها

کتابخانه‌های مورد نیاز (از قبل در requirements.txt موجود است):

```
mnemonic==0.21
eth-account
bip-utils==2.9.3
```

## 🚀 نحوه استفاده

### روش 1: اجرای مستقیم

```bash
python bsc_wallet_generator.py
```

### روش 2: اجرای با انتخاب (پیشنهادی)

```bash
python run_wallet_generator.py
```

این روش دو گزینه ارائه می‌دهد:
- **تست سریع**: تولید 10 کیف پول برای بررسی
- **تولید کامل**: تولید 100,000 کیف پول

## 📁 خروجی

فایل `bsc_wallets.csv` شامل ستون‌های زیر تولید می‌شود:

| ستون | توضیح |
|------|-------|
| `index` | شماره ترتیبی کیف پول |
| `mnemonic` | 12 کلمه بازیابی |
| `private_key` | کلید خصوصی (hex format) |
| `address` | آدرس عمومی BSC |

### نمونه خروجی:

```csv
index,mnemonic,private_key,address
1,"abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon about",0x...,0x...
2,"ability able about above absent absorb abstract absurd abuse access accident account",0x...,0x...
```

## ⚡ عملکرد

- **زمان تولید**: حدود 2-3 ثانیه برای هر 1000 کیف پول
- **حجم فایل**: تقریباً 25-30 MB برای 100,000 کیف پول
- **حافظه مصرفی**: کم (پردازش دسته‌ای)

## 🔒 نکات امنیتی

⚠️ **بسیار مهم**:

1. **هرگز کلیدهای خصوصی را به اشتراک نگذارید**
2. **فایل CSV حاوی اطلاعات حساس است - در مکان امن نگهداری کنید**
3. **از بکاپ گیری فراموش نکنید**
4. **فایل را رمزگذاری کنید**
5. **دسترسی فایل را محدود کنید**

## 🛠 تنظیمات قابل تغییر

در فایل `bsc_wallet_generator.py`:

```python
WALLET_COUNT = 100000      # تعداد کیف پول
OUTPUT_FILE = "bsc_wallets.csv"  # نام فایل خروجی  
BATCH_SIZE = 1000          # اندازه دسته برای ذخیره
PROGRESS_INTERVAL = 5000   # فاصله نمایش پیشرفت
```

## 🔧 عیب‌یابی

### خطای import:
```bash
pip install mnemonic eth-account bip-utils
```

### خطای حافظه:
- `BATCH_SIZE` را کاهش دهید
- `WALLET_COUNT` را کم کنید

### خطای ذخیره فایل:
- مجوز نوشتن در پوشه را بررسی کنید
- فضای دیسک کافی داشته باشید

## 💡 استفاده در شبکه‌های دیگر

این کیف پول‌ها با شبکه‌های EVM-compatible زیر نیز سازگار هستند:

- ✅ Ethereum (ETH)
- ✅ Polygon (MATIC) 
- ✅ Avalanche (AVAX)
- ✅ Fantom (FTM)
- ✅ Arbitrum (ARB)

## 📞 پشتیبانی

در صورت بروز مشکل، لطفاً موارد زیر را بررسی کنید:
1. نسخه Python 3.7+
2. نصب صحیح کتابخانه‌ها
3. مجوزهای فایل سیستم
4. فضای دیسک کافی

---

**⚠️ هشدار**: این ابزار صرفاً برای اهداف آموزشی و توسعه طراحی شده است. استفاده در محیط production مسئولیت کاربر است.
