# 🔑 راهنمای تنظیم API Key برای Polygon

## 📋 مراحل دریافت API Key از PolygonScan

### 1️⃣ **ثبت نام در PolygonScan**
- به سایت [PolygonScan.com](https://polygonscan.com/) بروید
- روی **"Sign In"** کلیک کنید
- **"Click to sign up"** را انتخاب کنید
- ایمیل و رمز عبور تعیین کنید

### 2️⃣ **ایجاد API Key**
- پس از ورود، روی نام کاربری خود کلیک کنید
- **"API Keys"** را انتخاب کنید  
- **"+ Add"** را کلیک کنید
- نام برای API key تعیین کنید (مثل: "Transaction Finder")
- **"Create"** را کلیک کنید

### 3️⃣ **کپی API Key**
- API key شما نمایش داده می‌شود
- آن را کپی کنید (فقط یک بار نمایش داده می‌شود!)

## ⚙️ تنظیم در پروژه

### 4️⃣ **ایجاد فایل .env**
```bash
# در مسیر پروژه این فایل را ایجاد کنید
echo "POLYGONSCAN_API_KEY=your_actual_api_key_here" > .env
```

### 5️⃣ **مثال .env**
```bash
# فایل .env
POLYGONSCAN_API_KEY=ABC123XYZ789...
```

## 🚀 اجرای اسکریپت با API Key واقعی

### 6️⃣ **اجرا با تنظیمات شبکه اصلی**
```bash
# نصب وابستگی‌ها
pip install -r requirements_polygon.txt

# اجرای اسکریپت
python polygon_transaction_finder.py
```

## 📊 نتایج مورد انتظار

با API key واقعی، اسکریپت:
- ✅ **20 بلاک** آخر Polygon را بررسی می‌کند
- ✅ تا **1000 تراکنش** آنالیز می‌کند  
- ✅ فی‌های **$0.01 تا $20** را در نظر می‌گیرد
- ✅ تا رسیدن به **$52 کل فی** ادامه می‌دهد
- ✅ حداکثر **50 تراکنش** برمی‌گرداند

## ⚠️ نکات مهم

### 🔐 امنیت API Key
- هرگز API key را در کد commit نکنید
- فایل `.env` را در `.gitignore` قرار دهید
- API key خود را با کسی به اشتراک نگذارید

### 📈 محدودیت‌های API
- **PolygonScan Free**: 5 درخواست در ثانیه
- **Rate Limiting**: اسکریپت خودکار delay اعمال می‌کند
- **Daily Limit**: حدود 100,000 درخواست در روز

### 🎯 کرایتریا "New Wallet" 
- **موجودی فعلی**: ≤ 500 POL (~$225)
- **کل دریافتی**: ≤ 2000 POL (~$900)  
- **صرافی‌های مجاز**: Binance, XT.com
- **مقدار تراکنش**: < $300

## 🛠️ عیب‌یابی

### ❌ خطای "Invalid API Key"
```bash
# چک کنید که API key در .env درست باشد
cat .env

# API key نباید فاصله اضافی داشته باشد
POLYGONSCAN_API_KEY=ABC123...
```

### ❌ خطای "Rate Limit"
- اسکریپت خودکار delay اعمال می‌کند
- صبر کنید تا محدودیت برطرف شود

### ❌ "No transactions found"
- کرایتریا ممکن است خیلی سخت باشد
- تاریخ‌های آینده (2025) شبیه‌سازی می‌شوند

## 📱 تماس و پشتیبانی

اگر مشکلی داشتید:
1. API key را دوباره چک کنید
2. فایل `.env` را بررسی کنید  
3. اتصال اینترنت را تست کنید
4. لاگ‌های خطا را بررسی کنید

---

**✨ با API key واقعی، به راحتی به هدف $52 فی خواهید رسید! 🚀**
